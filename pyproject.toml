[project]
name = "telegram-bot"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "aiogram>=3.0",
    "python-dotenv>=1.0.0",
    "pydantic-settings>=2.0.0",
]

[tool.uv]
dev-dependencies = []